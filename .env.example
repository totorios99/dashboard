# SQLite DB path — for Docker, point to a volume mount
DATABASE_URL="file:./prisma/cockpit.db"

# Override vault root for Docker deployments
# VAULT_ROOT=/vaults

# Override photos directory for Docker deployments
# PHOTOS_DIR=/photos
