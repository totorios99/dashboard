# SQLite DB path — relative paths resolve from prisma/ (schema dir).
# For Docker, point to a volume mount.
DATABASE_URL="file:./cockpit.db"

# Override vault root for Docker deployments
# VAULT_ROOT=/vaults
